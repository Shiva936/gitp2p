[package]
name = "gitp2p-vault"
description = "Vault, repository, and checkpoint lifecycle for gitp2p"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gitp2p-metadata = { workspace = true }
gitp2p-trust = { workspace = true }
